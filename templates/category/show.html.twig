{% extends 'base.html.twig' %}

{% block title %}{{ category.name}}{% endblock %}

{% block body %}

    {# <main class="container"> #}
  
        <div class="row g-5 alignement">
            <div class="">
                <h3 class="pb-4 mb-4 fst-italic">{{ category.name }}</h3>
                <div id="article-list">
                     <div class="categorylist ">
                {% include 'article/list.html.twig' with {articles:category.articles } %}
                </div>
                </div>
            </div>
        
        </div>
    

    {# </main> #}

<!-- sur le fichier show on affiche le nom de la catégorie et on inclu le fichier liste des articles puis la relation entre categorie et liste -->
{% endblock %}

