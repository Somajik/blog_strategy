{% extends 'base.html.twig' %}

{% block title %}{{article.title}}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ article.title }}</h1>
    <hr><!-- HR ligne horizontale -->
        <p>{{ article.content|raw}}</p> <!-- filtre twig raw -->
    </div>

{% endblock %}
