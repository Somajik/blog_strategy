

{% set username = comment.user.username %}

    <div class="comment mb-5" id="comment-{{ comment.id }}">
        <h5 class="card-title">
            <a class="text-decoration-none text-white" href="#">
            <span class="comment-author text-white">{{ username }}</span>
            </a> {{ comment.createdAt|date('d M Y à H:i:s') }}
        </h5>

        <p class="card-text text-white">{{ comment.content }}
        </p>
</div>
